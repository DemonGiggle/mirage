networkPolicy:
  version: 1
  loopback:
    default: allow
  ingress:
    default: deny
    rules: []
  egress:
    default: deny
    rules:
      - action: allow
        destination:
          cidr: 203.0.113.0/24
        protocol: any
        ports: [443]
